# Code-Review-Aufgabenliste

Gepflegte To-do-Liste aus Code-Reviews (siehe AGENTS.md). Erledigte Punkte werden
mit ✅, Datum und kurzem Kommentar markiert.

Hinweis: Die ursprüngliche Datei mit den Einträgen 1–16 war nicht im Repository
und ist verloren gegangen. Die Nummern 17 und 18 sind aus den Verweisen in
AGENTS.md rekonstruiert; neue Einträge werden ab 19 fortgezählt.

| Nr. | Thema | Status |
| --- | --- | --- |
| 17 | ESLint-Altbestand: bekannter Rückstand an Lint-Findings im Bestandscode. Keine neuen Findings einführen; Altbestand nicht nebenbei mitfixen. | ✅ 2026-07-07: `npm run lint` läuft ohne Findings — der Altbestand ist bereits abgebaut, kein offener Rückstand mehr. |
| 18 | knip-Altbestand: gemeldete tote Dateien/Exports/Typen (`npm run check:unused`); False-Positive-Einstiegspunkte stehen in `knip.json`. | ✅ 2026-07-07: letzte drei nur intern genutzte Exports (settings.ts, comfy/defaultNarratorVoice.ts) auf modul-lokal umgestellt; knip meldet nichts mehr. |
| 19 | Custom Node: Nutzer-Code läuft ohne Zeitlimit im UI-Hauptthread (`src/nodes/custom-node/runtime.ts`). Eine Endlosschleife (z. B. vom Assistenten generiertes `while(true)`) friert die ganze App ein. Lösung: Ausführung in einen Web Worker mit Timeout auslagern; die `llm`/`llmJson`-Aufrufe müssen dann per Message-Passing zum Hauptthread zurück. Größerer Umbau. (Gefunden im Custom-Node-Review am 2026-07-05.) | offen |
| 20 | Custom Node: graph-run failures from `executeCustomNode` are only shown through the graph runtime error path and are not automatically added to the Custom Node Assistant chat history. Manual Run button failures are covered, but full workflow-run failures should also become assistant context when a Custom Node Assistant history exists for that node. | ✅ 2026-07-05: Custom Node run errors are now collected as dismissible Assistant diagnostics and included in Assistant context. |
| 21 | Custom Node Assistant preview uses a mock LLM response instead of the selected provider, so LLM-based Custom Nodes can appear to work in preview even when the real provider call would fail. Either label this clearly as a mock preview or add an optional real-provider preview run. | open |
| 22 | Voice privacy: deleting generated ComfyUI voice outputs fails silently. `deleteComfyOutputFile` in `electron/main.cjs` ignores its result, so when the ComfyUI build has no delete route the user believes clips are removed while they stay in the ComfyUI output folder. Surface a one-time warning (or health detail) when deletion fails. | ✅ 2026-07-06: deletion is now verified with a follow-up existence check; failures warn at most twice per provider, the second warning tells the user to delete the files manually. |
| 23 | Voice privacy: uploaded reference voice samples (`rpgraph-voice-<hash>.mp3`) accumulate forever in the ComfyUI input directory and are never deleted, even with "delete voice outputs" enabled. Consider deleting them after the run or documenting the leftover files. | ✅ 2026-07-06: with the delete option on, the uploaded sample is deleted (and verified) after every voice run, even when the run fails; failures use the same warning path as #22. |
| 25 | Image Generation Assistant: generated images live only in dialog state (`ImageGenerationAssistantDialog.tsx`). Closing the dialog (Close button or backdrop click) silently discards all generated but unsaved images without confirmation. Escape is now blocked while generating, but a confirmation prompt ("unsaved images will be lost") or keeping the session until the phone panel closes would be safer. (Found in the image assistant review on 2026-07-07.) | ✅ 2026-07-07: closing with unsaved generated images now requires explicit confirmation; empty or fully saved sessions close directly. |
| 26 | Image Generation Assistant: error routing between chat and settings tab relies on the error message string prefix `'Image Settings'` (`ImageGenerationAssistantDialog.tsx`, `submitMessage`/`handleGenerateImage`). Works, but breaks silently if the message wording changes; a typed error or error code would be more robust. (Found in the image assistant review on 2026-07-07.) | ✅ 2026-07-07: settings failures now use a dedicated `ImageSettingsError` instead of message-prefix matching. |
| 27 | Image Generation Assistant: the save-character menu has no outside-click handler — it only closes on selection, save success, or toggling the button. Clicking elsewhere in the dialog leaves it open. Minor UX. (Found in the image assistant review on 2026-07-07.) | ✅ 2026-07-07: the save-character menu now closes when the user clicks elsewhere. |
| 24 | Narrator-only early narration (`useDialogueVoice.ts`): when the run finishes while the API narration is still generating, `readMessagesAsNarrator` clears `earlyNarrationRef` before the clip exists, so the finished clip is never stored on the message (not replayable from the session). Also, the early-narration path returns after handling only the first output bubble of a turn, and the stored clip text uses the final message text while the audio was generated from the early RP Output text — these can drift apart. (Found in the voice code review on 2026-07-07.) | offen |
| 28 | Banking transfer integrity: a Phone Banking request is sent through the LLM and the returned transfer is booked without comparing it with the typed request. Keep a typed pending request through the graph run and verify the returned sender, recipient, amount, and note, or book the original request deterministically. Runtime checks now reject self-transfers, overdrafts, and transfers unrelated to Storybook accounts, but they cannot prove that the LLM copied the user's request. (Found in the Banking review on 2026-07-10.) | offen |
| 29 | Banking ledger identity: stored transfers identify both parties by display name only. Renaming a character can hide earlier transfers, while duplicate character names remain ambiguous. Store stable Storybook character ids for internal accounts and keep names only for display and external parties. (Found in the Banking review on 2026-07-10.) | offen |
| 30 | Banking statement consistency: generated and fixed expense rows look like real debits but intentionally do not affect the displayed balance, so the statement cannot be reconciled. Either model an explicit opening/current balance relationship or clearly separate decorative history from ledger transactions. (Found in the Banking review on 2026-07-10.) | offen |
| 31 | Social apps: `PhoneSocialFeedScreen.tsx` combines account onboarding, feed derivation, delayed animations, posting, comments, DMs, wallet controls, purchases, uploads, and image generation in one component of roughly 1,900 lines. Split domain calculations into tested helpers/hooks and extract focused feed, sidebar, composer, and wallet components. (Found in the Social Apps review on 2026-07-11.) | offen |
| 32 | Social apps: manually added people live only in component state and disappear when the user leaves Fotogram or OnlyFriends. Decide whether these are followed accounts or DM contacts, then persist them per character and app in the RP save. (Found in the Social Apps review on 2026-07-11.) | ✅ 2026-07-11: DM partners already persist through their timeline messages and are reconstructed when either side has sent a message; unused temporary additions do not need persistence. |
| 34 | Social account identity: Storybook editing can still store duplicate Fotogram or OnlyFriends usernames for different characters. Since posts and DM conversations match by username, duplicates can merge or misattribute activity. Add normalized, per-app uniqueness validation with a clear editor error. (Found in the Social Apps review on 2026-07-11.) | offen |
